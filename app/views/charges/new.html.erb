 <div class="row">
   <div class="col-md-12">
     <% if current_user.role == 'standard' %>
       <%= form_tag charges_path do %>
         <h4>Upgrade Your Account to Premium Membership</h4>
         <script class='stripe-button' src="https://checkout.stripe.com/checkout.js" data-key="<%= @stripe_btn_data[:key] %>" data-amount=<%= @stripe_btn_data[:amount] %> data-description="<%= @stripe_btn_data[:description] %>" ></script>
       <% end %>
     <% else %>
       <p>You are a premium user</p>
       <%= link_to 'Downgrade Account', charge_path(id:1), method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to cancel your Premium membership? Doing so makes all your private wikis go public.'} %>
     <% end %>
   </div>
 </div>

 <div class="row">
   <div class="col-md-12 mt-10">
     <%= link_to "Back to Wikis", wikipages_path, class: 'btn btn-info' %>
   </div>
 </div>
