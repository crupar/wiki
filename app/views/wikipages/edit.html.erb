<h1>Edit Entry</h1>

<div class="row">
  <div class="col-md-4">
    <p>Guidelines for wiki entries</p>
    <ul>
      <li>Search Blocipedia in case an article already exists on the subject</li>
      <li>Gather sources</li>
      <li>Must have a unique title.</li>
    </ul>
  </div>

  <div class="col-md-8">
    <%= render partial: 'form', locals: { wikipage: @wikipage } %>
  </div>


  <% unless current_user.standard? %>
    <h4>Add collaborators </h4>
    <% @users.each do |user| %>
      <div id="pagination_table">
        <%= check_box_tag "wikipage[user_ids][]", user.id, @wikipage.collaborator_users.include?(user) %>
        <%= label_tag dom_id(user), user.username %></li>
      </div>
    <% end %>
  <% end %>


</div>
