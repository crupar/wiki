# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  secret_key_base: 53b21da48781a53c7f3c9e2513ad0c19373478b559b34c032f08333f1f76a4859be8d03c34ec3d8b3ed0edc3086fb6d3cde282e842568739d5abd4a73dc7be55

test:
  secret_key_base: f5e7831a572e35814314e5e65a1e52f714d74024ab282bd66a3abfa15286ba855720c9f8ccfcb2f1afd17255cb66c8ed12a475947ee494e754d345aec5414098

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>






# from github.com/railsapps/rails-devise-pundit
# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.


development:
  admin_name: Carolyn
  admin_email: carolyntestingcode@gmail.com
  admin_password: admin
  email_provider_username: <%= ENV["carolyntestingcode"] %>
  email_provider_password: <%= ENV["admin"] %>
  domain_name: example.com
  secret_key_base: very_long_random_string

test:
  secret_key_base: very_long_random_string

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  admin_name: <%= ENV["ADMIN_NAME"] %>
  admin_email: <%= ENV["ADMIN_EMAIL"] %>
  admin_password: <%= ENV["ADMIN_PASSWORD"] %>
  email_provider_username: <%= ENV["GMAIL_USERNAME"] %>
  email_provider_password: <%= ENV["GMAIL_PASSWORD"] %>
  domain_name: <%= ENV["DOMAIN_NAME"] %>
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
